name: eslint-check

on:
    pull_request:
        branches: [ master ]

jobs: 
    check-code: 
        name: ESLint check 
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
            - runs: npm ci
            - runs: npm run lint
